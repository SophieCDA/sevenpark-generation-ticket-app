import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: "center"
    },
    title: {
        marginBottom: 10,
        fontSize: 24,
        textAlign: "center"
    },
    details: {
        marginTop: 10,
        alignItems: "center"
    },
    error: {
        marginTop: 10,
        alignItems: "center"
    }
})